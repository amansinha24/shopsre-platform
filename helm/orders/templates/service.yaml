apiVersion: v1
kind: Service
metadata:
  name: orders-service
  namespace: production
spec:
  type: {{ .Values.service.type }}
  selector:
    app: orders-service
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
