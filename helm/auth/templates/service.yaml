apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: production
spec:
  type: {{ .Values.service.type }}
  selector:
    app: auth-service
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
